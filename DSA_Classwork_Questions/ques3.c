#include<stdio.h>
#include<string.h>

#define STUDENTS 100
#define SUBJECTS 6
#define NAME 50

double avg(int marks[]{
    int sum=0;
    for(int i=0;i<SUBJECTS;i++)
        sum += marks[i];
    return (double)sum/SUBJECTS;
}
void sortbyRank(double averages[], int ranks[]{
    for(int i=0; i<STUDENTS;i++){
        ranks[i] = i;
    }
    while(j>=0 && averages[ranks[j]]<a)


}